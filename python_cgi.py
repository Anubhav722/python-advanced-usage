# CGI stands for Common Gateway Interface
# CGI is the standard for programs to interface with HTTP Servers.
# This means we can take information from things like forms.

# Let's setup a simple default web server on Ubuntu Apache 2

import socket
